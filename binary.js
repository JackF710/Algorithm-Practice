/*
    take binary number
    count starting at 2^0 and loop until 2^7
    */

function binaryDecimal(b, binary) {
  if (b > 8) {
    return;
  }
  let exponent = 0;
  let binary = 2;
  for() {
      if (b === "1") {
      exponent++;
    }
  }
  return i;
}

console.log(binaryDecimal(8, "10010110"));
